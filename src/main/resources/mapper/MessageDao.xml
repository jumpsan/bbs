<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.bbs.dao.MessageDao">

    <!--查询消息内容-->
    <select id="selectContent" resultType="message">
        select
        *
        from t_message
        where receive_id = #{receive_id} and send_id=#{send_id}
        limit #{start},#{num}
    </select>
    <!--新增所有列-->
    <insert id="addMessage" keyProperty="id" useGeneratedKeys="true">
        insert into bbs.t_message(content, send_time, receive_id, send_id)
        values (#{content}, #{sendTime}, #{receiveId}, #{sendId})
    </insert>
</mapper>